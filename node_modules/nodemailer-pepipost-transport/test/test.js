'use strict';
const nodemailer = require('nodemailer');
const pepipostTransport = require('../index');
const transporter = nodemailer.createTransport(pepipostTransport ({
    auth: {
        api_key: 'cdf2fcdce8a861df13d0423a0d478c83'
    }
}));

const mail = {
    from: 'Pusthaka <no-reply@pp.pusthaka.xyz>',
    to: ['4evercreations@gmail.com'],
    subject: 'Test Emailer',
    html: '<p> hi, this is a test email sent via Pepipost JSON API.</p>',
};
transporter.sendMail(mail, function(err, info) {
    if (err) {
        console.log(err);
    }
        console.log(info);
});